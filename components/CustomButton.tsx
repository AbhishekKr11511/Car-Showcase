"use client"

import { useState } from "react"

const CustomButton = () => {


    
  return (
    <button
        disabled={false}
        type="button"
        className={`custom-btn`}
        onClick={()=>{}}
    >
        View
    </button>
  )
}
export default CustomButton