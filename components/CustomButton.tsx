"use client"

import { useState } from "react"

const CustomButton = () => {
    
  return (
    <button
        disabled={false}
        type="button"
        className={`custom-btn`}
        onClick={()=>{}}
        
    >
        <span className="flex-1">
            View
        </span>
    </button>
  )
}
export default CustomButton